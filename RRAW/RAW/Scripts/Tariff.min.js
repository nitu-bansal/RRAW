/// <reference path="http://ajax.googleapis.com/ajax/libs/jquery/1.6.2/jquery.min.js" />

function hasClass(ele, cls) {
	return ele.className.match(new RegExp('(\\s|^)' + cls + '(\\s|$)'));
}

function addClass(ele, cls) {
	if (!this.hasClass(ele, cls)) ele.className += " " + cls;
}

function removeClass(ele, cls) {
	if (hasClass(ele, cls)) {
		var reg = new RegExp('(\\s|^)' + cls + '(\\s|$)');
		ele.className = ele.className.replace(reg, ' ');
	}
}

function DisplayProgress() {
	document.getElementById('DivUpdateProgress').style.display = "inline";

	if (document.getElementById('gridTariff') != null) {
		document.getElementById('gridTariff').disabled = true;
	}

	if (document.getElementById('gridAdhocLanes') != null) {
		document.getElementById('gridAdhocLanes').disabled = true;
	}
}



$("input[type='text']").focus(function () {
	DisplayOperations();
});

$("#divTariff").scroll(function () {
	if ($("#divTariff").scrollTop() == 0) {
		DisplayOperations();
	}
	else {
		$("#divOperations").hide();
		$("#tdOperations").css("height", "0px");
		$("#divOperationAdjustments").css("height", "100px");
	}
});

function DisplayOperations() {
	$("#divOperations").show();
	$("#tdOperations").css("height", "40px");
	$("#divOperationAdjustments").css("height", "130px");
}

(document).ready(function () {
    addReflections();
})